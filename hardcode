from hub import light_matrix
from motor import velocity
import runloop
import motor_pair
from hub import port
import color_sensor
import distance_sensor
import color
import motor
import time
from hub import sound

score = 0
motor_pair.pair(motor_pair.PAIR_1, port.C, port.D)

async def main():
    motor_pair.move_for_time(motor_pair.PAIR_1, 5000, 0, velocity=360)
runloop.run(main())
